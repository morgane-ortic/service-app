from django.db import models
from django.contrib.auth.models import User

# The user model doesn't need to be defined because it's built-in
# We import it to use some of the fields in the Customer model